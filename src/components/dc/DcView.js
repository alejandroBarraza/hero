import React from 'react';
import { HeroesList } from '../heroes/HeroesList';

export const DcView = () => {
    return (
        <div>
            <HeroesList publisher={'DC Comics'} />
        </div>
    );
};
